'use strict';
const myName = 'Іванна';
const mySurname = 'Ткаченко';

if (myName.length > 4 && mySurname.length > 5) {
    console.log(myName.length + mySurname.length);
} else {
    console.log('УПС');
}